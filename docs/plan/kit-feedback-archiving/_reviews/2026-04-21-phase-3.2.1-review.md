---
제목: kit-feedback-archiving Phase 3.2.1 Review — matcher + 이슈 감지기 6종
작성일: 2026-04-21
리뷰어: Claude (메인테이너 역할)
상태: reviewed
---

# Phase 3.2.1 Review

> **결론**: 23 커맨드 화이트리스트 matcher + 6종 이슈 감지기 (permission_mismatch/framework_drift/cache_error/checkpoint_imbalance/duplication/trust_only) 순수 함수 구현. 21 tests passed (누적 175). Phase 3.2.2 (plan-collector) 진입 조건 충족.

## 구현 범위

### 신규 파일

| Claude | Codex |
|--------|-------|
| `src/claude/core/collectors/issue-detectors.js` | `src/codex/core/collectors/issue-detectors.js` |
| `tests/claude/core/collectors/issue-detectors.test.js` | — |

### 수정 파일

- `scripts/setup.js`: Stop 훅에 23 커맨드 matcher 화이트리스트 정교화 (Phase 3.1의 matcher 없음 → Phase 3.2.1의 정규식)

### 내보내는 API (순수 함수)

```
extractDomain(command) → 'plan'|'copy'|'dev'|'core'
detectPermissionMismatch(input) → {detected, type, severity, description, evidence, related_imp_kit_id}
detectFrameworkDrift(input)
detectCacheError(input)
detectCheckpointImbalance(input)
detectDuplication(input)   — IMP-KIT-017 유틸 재사용
detectTrustOnly(input)
detectAllIssues(input) → [{id: 'issue-001', type, severity, ...}, ...]
상수: ISSUE_TYPES (10종), SEVERITY_LEVELS (P0/P1/P2)
```

## TDD 이력

- **RED**: 21 케이스 작성 → 초기 2 실패
- **피드백 반영 1**: `detectCheckpointImbalance` 경계 — 5회를 P0에서 P1로 조정 (7회 이상을 P0로)
- **피드백 반영 2**: `detectTrustOnly` — "without" 부정 키워드 고려하는 lookbehind 정규식 추가
- **GREEN**: 21/21 passed

## 설계 문서 대비

| 03-trigger-points §2 matcher | ✅ 23 커맨드 화이트리스트 반영 |
| 02-feedback-schema §2.5.1 10 issue types | ✅ 상수 export (detector는 6종, 나머지 4종은 Phase 3.2+에서 필요 시 추가) |
| 01-architecture §4.1 1차 휴리스틱 | ✅ 규칙 기반, LLM 없음 |

## 제한 사항

- **IMP-KIT 연계**: detectPermissionMismatch → IMP-KIT-001, detectFrameworkDrift → IMP-KIT-002, detectCacheError → IMP-KIT-005, detectCheckpointImbalance → IMP-KIT-016, detectDuplication → IMP-KIT-017, detectTrustOnly → IMP-KIT-011
- **미구현 4 types**: missing_agent, undefined_mode, truncation_ignored, other — Phase 3.2+ 필요 시 추가
- **feedback-collector main() 미통합**: 현재 detector는 export만, 실제 훅 실행 시 호출은 Phase 3.2.5

## 검증

- `pnpm test`: 175 passed (14 test files, 995ms)
- matcher 정규식 유효성: `^/(plan-(...)|copy-(...)|dev-(...))($|\\s)` — 23 커맨드 명시
- extractDomain 4 케이스 통과 (plan/copy/dev/core)

## 변경 이력

| 일시 | 변경 | 작성자 |
|------|------|--------|
| 2026-04-21 | Phase 3.2.1 구현 리뷰 (TDD + 피드백 2건 반영) | Claude (메인테이너 역할) |
