---
제목: kit-feedback-archiving Phase 3.1 Review — 기본 인프라
작성일: 2026-04-21
리뷰어: Claude (메인테이너 역할)
대상: feedback-collector 기본 + feedback-entry schema v1.0
상태: reviewed
---

# kit-feedback-archiving Phase 3.1 Review

> **결론**: Phase 3.1 기본 인프라 완료. 19 tests passed (누적 151). 순수 함수 5종 (buildEntryId/applyRedactions/buildBaseEntry/validateFeedbackEntry/archivePath/writeEntry) + JSON Schema v1.0 + Stop 훅 등록. Phase 3.2 도메인별 수집 + Phase 3.3 index/stats는 후속 세션.

## 구현 범위

### 신규 파일

| Claude | Codex |
|--------|-------|
| `src/claude/core/hooks/feedback-collector.js` | `src/codex/core/hooks/feedback-collector.js` |
| `src/claude/core/_schemas/feedback-entry.schema.json` | `src/codex/core/_schemas/feedback-entry.schema.json` |
| `tests/claude/core/hooks/feedback-collector.test.js` | — |

### 수정 파일

- `scripts/setup.js`: Stop 훅에 `feedback-collector.js` 등록 (core 도메인, 항상 활성)

### 테스트 (19건)

- `buildEntryId` 3건 (네이밍 규칙)
- `applyRedactions` 6건 (macOS/Linux/Windows 홈 + API 키 + 비민감 + 상수)
- `buildBaseEntry` 3건 (필수 필드 + usage + metadata)
- `validateFeedbackEntry` 5건 (유효/누락/enum/형식)
- `archivePath` 2건 (claude/codex runtime)

## 설계 문서 대비 커버리지

| 설계 | 구현 상태 |
|------|:---:|
| 02-feedback-schema §2 Top-level 필드 | ✅ (buildBaseEntry + schema) |
| 02-feedback-schema §7 Redaction | ✅ (applyRedactions 5 패턴) |
| 04-archive-layout §1 디렉터리 구조 | ✅ (archivePath) |
| 04-archive-layout §2 파일 네이밍 | ✅ (buildEntryId) |
| 04-archive-layout §3 index.md | ⏳ Phase 3.3 |
| 04-archive-layout §4 stats.json | ⏳ Phase 3.3 |
| 03-trigger-points 도메인별 수집 | ⏳ Phase 3.2 |
| 05-codex-vs-claude fallback Skill | ⏳ 미확인 (Codex v1 Stop 훅 호환성) |

## 제한 사항 (Phase 3.2/3.3로 이월)

- **main() no-op**: 현재 훅이 stdin 파싱 후 exit 0만 수행. 실제 엔트리 생성은 Phase 3.2에서.
- **도메인별 수집기 부재**: plan/copy/dev/core 특화 이슈 감지 로직 미구현.
- **index/stats 자동 생성 부재**: Phase 3.3에서 `updateIndex()` + `regenerateIndexMd()`.
- **Codex Stop 훅 호환성**: 05-codex-vs-claude.md 권고대로 Skill fallback artifact 전환 가능성 재검토.

## 검증

- `pnpm test`: **151 passed** (13 test files, 868ms)
- Schema ajv 컴파일 성공 (feedback-entry.schema.json)
- 훅 등록: `buildHooksConfig`의 Stop 배열에 feedback-collector 추가

## 변경 이력

| 일시 | 변경 | 작성자 |
|------|------|--------|
| 2026-04-21 | Phase 3.1 기본 인프라 리뷰 | Claude (메인테이너 역할) |
