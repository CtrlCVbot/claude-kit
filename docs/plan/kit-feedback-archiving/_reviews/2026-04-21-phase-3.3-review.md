---
제목: kit-feedback-archiving Phase 3.3 Review — index/stats/rollup 전체 완료
작성일: 2026-04-21
Phase: 3.3 (3.3.1 ~ 3.3.4 통합)
상태: reviewed
---

# Phase 3.3 Review — **Phase 3 전체 완료**

> **결론**: stats.json 집계 + index.md 렌더 + 월 단위 롤업 + rebuild 스크립트 완성. **Phase 3 (3.1+3.2+3.3) 전체 완료**. 248 tests passed. feedback-archiving 시스템이 end-to-end로 엔트리 생성·저장·집계·인덱스 출력 가능.

## 구현 (3.3.1 ~ 3.3.4 통합)

### 3.3.1 stats-aggregator

| 파일 | 역할 |
|------|------|
| `src/claude/core/archive/stats-aggregator.js` | defaultStats + updateStats (증분) + buildStatsFromEntries (전체) |
| `src/codex/core/archive/stats-aggregator.js` | Codex sibling |

핵심 함수:
- `defaultStats()`: 초기 구조
- `updateStats(existing, entry)`: 증분 갱신 (불변성: 새 객체 반환)
- `buildStatsFromEntries(entries[])`: 전체 재계산

집계: total_entries, by_runtime, by_domain, by_severity, by_imp_kit, top_issue_types (count 내림차순), latest_entry_ids (최대 10)

### 3.3.2 index-renderer

| 파일 | 역할 |
|------|------|
| `src/claude/core/archive/index-renderer.js` | renderIndexMd(stats) → index.md 마크다운 |
| `src/codex/core/archive/index-renderer.js` | Codex sibling |

출력 섹션: 헤더 + 통계 + 최근 10건 테이블 + IMP-KIT 집계 테이블.
경고 메시지: "자동 생성 — 직접 편집 금지"

### 3.3.3 rollup-generator

| 파일 | 역할 |
|------|------|
| `src/claude/core/archive/rollup-generator.js` | groupEntriesByMonth + buildMonthlyRollup + renderRollupMd |
| `src/codex/core/archive/rollup-generator.js` | Codex sibling |

entry_id 앞 8자리(YYYYMMDD)에서 YYYY-MM 추출 → 월 그룹화 → 월별 통계 + 도메인/이슈/IMP-KIT 요약 렌더.

### 3.3.4 rebuild 스크립트

`scripts/feedback-rebuild-index.js`:
- `.claude/feedback-archive/**/*.json` 재귀 탐색
- stats.json 재생성 + index.md 재생성 + _rollups/{YYYY-MM}.md 월별 생성
- `--month 2026-04` 옵션으로 특정 월만
- 아카이브 부재 시 skip + exit 0

## 테스트 (21건)

- stats-aggregator 10 (defaultStats + updateStats 6 + buildStatsFromEntries 3)
- index-renderer 3 (헤더 + 경고 + 빈 stats)
- rollup-generator 8 (그룹화 2 + 롤업 3 + 렌더 1 + 통합 2)

누적: 248 tests passed (1.02s)

## Phase 3 종합 (전체 완료)

| Sub-phase | 산출물 | 테스트 |
|:---:|--------|:----:|
| 3.1 | feedback-collector 기본 + feedback-entry.schema.json | 19 |
| 3.2.1 | issue-detectors 6종 + 23 커맨드 matcher | 21 |
| 3.2.2 | plan-collector 9 커맨드 | 17 |
| 3.2.3 | copy-collector 7 커맨드 | 11 |
| 3.2.4 | dev-collector + SubagentStop aggregator | 16 |
| 3.2.5 | buildFullEntry 통합 + SubagentStop wrapper | 9 |
| 3.3 | stats + index + rollup + rebuild | 21 |
| **합계** | **40+ 파일** | **114 Phase 3 tests** |

## 설계 문서 대비

- [04-archive-layout §3 index.md](../04-archive-layout.md#3-indexmd-구조): ✅ renderIndexMd
- [04-archive-layout §4 stats.json](../04-archive-layout.md#4-statsjson-구조): ✅ stats-aggregator
- [04-archive-layout §5 월 롤업](../04-archive-layout.md#5-월-단위-롤업): ✅ rollup-generator
- [04-archive-layout §8.2 수동 재계산](../04-archive-layout.md#8-인덱스-갱신-로직): ✅ feedback-rebuild-index.js

## 제한 사항 (Phase 4+ 이월)

- **runtime main() 경로 활성화**: feedback-collector.js의 main()이 현재 구조적 no-op. Claude Code runtime에서 훅에 전달되는 데이터(command, transcript 경로, session_id) 확정 후 활성화.
- **dash-preview-phase3 복제 회귀**: 실제 세션 실행하여 엔트리 누적·통계 정확성 검증. 별도 세션 영역.
- **증분 hook 자동화**: 각 엔트리 저장 직후 stats.json 증분 갱신하는 훅 체인 (현재는 `feedback-rebuild-index.js` 수동 실행)
- **Codex v1 Stop 훅 호환성**: 05-codex-vs-claude.md 권고대로 실제 환경 테스트 후 fallback Skill 필요 시 전환

## 검증

- pnpm test: 248 passed (20 test files, 1.02s)
- rebuild script 실제 실행: 아카이브 부재 시 안전 skip + exit 0
- index-renderer markdown 출력 검증 (헤더/통계/경고 포함)

## 변경 이력

| 일시 | 변경 | 작성자 |
|------|------|--------|
| 2026-04-21 | Phase 3.3 (3.3.1~3.3.4) 통합 구현 + Phase 3 전체 완료 | Claude (메인테이너 역할) |
