{
  "_note": "이 파일은 참조용 템플릿입니다. 실제 settings.json은 setup.js의 buildSettingsTemplate()이 도메인 조건부로 동적 생성합니다.",
  "permissions": {
    "allow": ["Read", "Edit", "Write", "Glob", "Grep", "Bash"],
    "deny": ["WebFetch", "WebFetch(*)"]
  },
  "hooks": {
    "_comment": "setup.js buildHooksConfig()가 activeDomains 기반으로 동적 생성"
  },
  "env": {
    "ENABLE_TOOL_SEARCH": "auto:5",
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  }
}
