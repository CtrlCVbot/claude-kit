## 날짜와 시간

날짜·요일·기간·D-day·일정 계산은 머릿속으로 하지 않습니다. language runtime 또는 system date 도구로 확인하고, 가능한 한 절대 날짜를 함께 적습니다.

Today's date is {{DATE}}.

> macOS/Linux `date` 명령과 Python `datetime` 권장 패턴은 `.claude/rules/date-calculation.md` 참조.
