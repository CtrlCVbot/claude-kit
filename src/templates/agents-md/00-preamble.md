## 이 문서의 역할

이 파일은 설치된 프로젝트에서 Codex가 먼저 읽는 runtime guidance입니다. 모든 판단은 `claude-kit` 내부 authoring source가 아니라 현재 프로젝트의 실제 파일, 설정, 테스트 결과를 기준으로 합니다.

기존 프로젝트에 이미 `AGENTS.md`가 있으면 설치 과정에서 managed 영역만 업데이트되고 그 외 사용자 편집은 보존됩니다.
