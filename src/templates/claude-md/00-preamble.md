## claude-kit 활성 구성

- domains: {{ACTIVE_DOMAINS}} | targets: {{ACTIVE_TARGETS}} | version: {{VERSION}}
- Kit 관리 영역. 아래 섹션들은 `pnpm install` (또는 `pnpm update claude-kit`) 실행 시 `postinstall` 훅을 통해 재생성된다. 직접 편집 지양.
