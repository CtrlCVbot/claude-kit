# Bridge Stitch: user-guide-website

## Stitch Decision

`review-only`.

Google Stitch is not used as an external artifact source in this restart. P6 exists to verify PRD/wireframe/content alignment and to prevent the design checkpoint from being skipped.

## Implementation Implication

| Item | Rule |
| --- | --- |
| Design source | Existing HTML guide plus current docs-first Next prototype |
| Stitch asset | None |
| Required validation | REQ/SCR mapping and content parity checks |
| Follow-up | External visual design can be a later feature if the docs site needs a new visual identity |
